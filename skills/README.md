# skills

Skill playbooks loaded on demand by `app/skills.py`.

Files here may be symlinks to a SKILL.md inside another repo on the local
machine so they stay in sync with their source. Those symlinks are gitignored
(their targets are machine-local), so a fresh clone starts with none.
